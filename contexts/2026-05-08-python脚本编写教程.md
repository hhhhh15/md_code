
# 📅 日期

2026-05-07

# common——use代码逻辑

## common1:生成新的sheet
  ### 代码
```python

    write_summary_sheet(wb, df_jp, only_in_jp, diff_zh_keys, jp_zh_map, feat_zh_map)
    ws = wb.create_sheet(title=OUTPUT_SHEET)  # 在原 wb 上新建 Sheet
    wb.save(INPUT_FILE)  # 保存回 cn_global.xlsx

```
**问题**
1.填写根据参考的main/value/string.xml是在哪里呢？

**回答**
1.在脚本里面的load_string_defs函数里面`if key not in defs or parent == 'values':defs[key] = val`，
逻辑：
    0.1.如果key存在则填写value，
    0.2.要是values/string.xml中也有，以这个为准。要是string.xml没有的话，还是有别的语言版本的这个key和value。
我觉得values/string.xml应该是完整的，所以0.2的情况不会有

### 展示样式

**需求**
1.我需要这个脚本代码能实现每次运行的时候能输出在指定的xlsx里面，加表单就行
2.


**做法流程**

---

# 脚本逻辑

## 脚本逻辑1：scanner_Rstring.py
1.遍历指定类名，从中找出R.string的所有的key，并通过key从main/string.xml中找到对应的value
2.再从当前类中的找到R.layout.xxx，遍历res/layout文件路径，找到对应的xml，从中找出所有的@string/xxxx。也是找到key，再从main/string.xml中找到对应的中文
3.将这些key和value按照表格形式输出，第一列是Key,第二列是简体中文，第三列是key出现的类名或则xml名，只出现一次，之后第四列是再第三列的旁边解释说明这个类是干嘛用的
4.注意：
    4.1.当前表单不要出现重复的key和value，算了，重复也无所谓了
    4.2.出现新的类，记得隔一行
5.升级：(不知道能不能用)
    5.1.能不能时间找到当前类名的Intent跳转之类的，然后跳转页面再执行像上面的那样
    5.2.不确定是不是跳转的类名，可以增加一个和终端交互的功能




---

## 脚本逻辑3：


## 脚本逻辑2：

需求是：
1.读取 cn_global.xlsx 的所有 Key
2.在 Android 项目源码里全局搜索 R.string.[key] 和 @string/[key]
3.两种都找不到的 = 废弃，找得到的 = 使用中
4.在原 xlsx 新增一个 Sheet 输出结果


## 逻辑修改思路
1.不是单个类，修改成一个路径，就是这个main/java/com/homerunpet/view/message  ,这个包下面的所有类扫一遍（当然，我需要梳理这些包下面的类对应的是什么UI）
2.还需要一个脚本1，能扫描我现在所有整理的sheet，有没有key是在多个表单的，【用标注还是整理表单？】
3.还需要一个脚本2，扫描所有sheet，就是去查看value，要是value是一样的，key不同的，整理在一个表单里面，第三列第四列原来格式，第五列标注出现在的表单sheet名
4.还需要一个脚本3，能帮我算出我现在还差多少key没有整理，对比all_string.xlsx表
5.可以先弄一个脚本4梳理出是哪些废弃中，哪些在使用，判断条件是，全局搜索R.string.[key]和@string/[key]都没有的，对比all_string.xlsx表


# 颜色区分
1.区分该key在多个sheet都有使用
2.



# python准则

